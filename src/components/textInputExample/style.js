import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    textBox:{
        fontSize: 24,
        color : "blue"

    },
    textInput:{
        fontSize: 18,
        color:'red',
        borderColor: 'red',
        margin:10,
        borderWidth: 2,
    }
})